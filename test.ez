# EzLang input

/* Multi-line comment
   Testing
   ignore
   End */

start() begin
    num x = 10
    dec y = 2.5
    char c = 'A'

    show("Start")

    check (x > 0 and y > 0) begin
        show("Both positive")
    end

    x = power(2, 3)
    show(x)

    num i = 0
    during (i < 3) begin
        show(i)
        i = i + 1
    end

    check (not x equals 0) begin
        show("Done")
    end

    send 0
end
